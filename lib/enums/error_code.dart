enum ErrorCode {
  /// The code that characterizes the rejection of the update by the user
  USER_DENIED_UPDATE,

  /// Another error caused the user to not consent to the update
  IN_APP_UPDATE_FAILED,
}